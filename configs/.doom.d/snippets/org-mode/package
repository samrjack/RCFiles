# -*- mode: snippet -*-
# name: package tangle
# condition: (string= (buffer-name) "emacs.org")
# key: package
# uuid: package
# --
#+begin_src emacs-lisp :tangle ../configs/.doom.d/packages.el
(package! `%`$0)
#+end_src
